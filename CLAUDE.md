# ml-lecture — 머신러닝·딥러닝 특강 페이지 유지보수 가이드

> 단일 HTML(`ml_dl_interactive.html`, 131KB)을 2026-07-08에 이 구조로 이식(Fable). 원본은 `backup/`에 보존.
> 앞으로의 계획은 `ROADMAP.md`(3개월 로드맵, 2026-07-14 신설 — 사용자 액션 대기 목록 포함) 참조.
> 정글(`C:\Users\정벅\jungle`)·마크다운(`C:\Users\정벅\markdown`)과는 **별개 프로젝트** — 그쪽 규칙(CSP·sw.js·이벤트 위임)을 끌어오지 말 것.

대상: 고등학생 정보 교과 시험 대비 인터랙티브 부교재. 톤 = 위트 있는 비유 + 시험 포인트.

**배포 경로 (2026-07-14 현행화)**:
- **주 배포 = GitHub Pages** https://jungbucks.github.io/ml-lecture/ — main에 푸시하면 Actions(`.github/workflows/deploy.yml`)가 **`src/` 폴더를 그대로 루트에 게시**한다. 즉 **push가 곧 라이브 배포**이므로 src가 미완성인 상태로 push 금지.
- 보조 배포 = `dist/ml_dl_interactive.html` 단일 파일(오프라인·게시판 파일 업로드용).

## 절대 규칙

1. **ES 모듈(import/export) 금지.** 학생 PC에서 file://(더블클릭)로 열리는 게 전제 — ESM은 file://에서 죽는다. 모든 JS는 **classic script + 전역 공유**(한 파일의 top-level `const`/`function`을 다른 파일이 그대로 사용). `<input type="module">`, fetch, CDN 라이브러리 전부 금지.
2. **수정 후 `node tools/build.mjs` 필수.** 빌드가 ① 매니페스트 파일 존재 ② JS 문법 ③ 미등록 파일까지 검문한다. 빌드 실패 상태로 끝내지 말 것.
3. **새 파일을 만들면 `src/index.html`의 `<script src>` 목록에 등록** (빌드가 미등록을 잡아주지만, 순서는 사람이 정한다). **로드 순서: data/* → js/core.js → js/sections/*(현재 순서 유지) → pagefx.js 맨 끝.** 엔진이 데이터 const를 로드 시점에 읽으므로 데이터가 먼저여야 한다(TDZ).
4. **데이터/엔진 분리 원칙**: 콘텐츠(문항·카드·비유·문구) 수정은 `src/data/`만 건드린다. 엔진(`src/js/`)을 같이 고쳐야 하는 콘텐츠 수정은 설계 신호 — 멈추고 재검토. 각 데이터 파일 머리에 스키마 주석이 있다.
5. **공유는 Pages URL 또는 dist 파일만.** 온라인은 Pages URL(문서 머리 참조 — push=배포임을 기억), 파일로 줄 땐 dist 단일 파일. src 폴더를 통째로 학생에게 주지 말 것. 홈의 `C:\Users\정벅\ml_dl_interactive.html`은 과거 배포 방식의 **스테일 사본(2026-07-09, 현재 dist보다 뒤처짐)** — 기준 아님, 갱신 의무도 없음.
6. **동기화 쌍 (한쪽만 고치면 안 되는 것들)**:
   - `data/ann-types.js`(매칭 게임) ↔ index.html ④-4 신경망 표 — 문구 일치
   - `data/steps7.js` ↔ index.html ① 7단계 목록 — 문구 일치 (게임 채점이 이 텍스트 기준)
   - ⚠️ 섹션 번호는 재배치 때마다 변한다(현행: ①7단계 ②도감 ③스케일링 ④딥러닝입문 ⑤하이퍼 ⑥코드 ⑦버그 ⑧코드퀴즈 ⑨프로젝트 ⑩자가진단 ⑪심화). 재배치 시 이 규칙의 번호와 데이터 파일 속 "섹션(N)" 참조 문구도 함께 갱신할 것
   - `data/quizzes.js`의 FINAL 문항 추가 시 FINAL_SEC(오답노트 섹션 매핑)도 추가
7. **학번·이름은 저장 금지**(개인정보 — 결과 요약 복사에만 사용). localStorage 자체를 현재 안 쓴다.
8. **작은 글씨 금지(사용자 확정 선호)** — 본문성 텍스트 0.85rem(≈13px) 미만으로 내리지 말 것. 위트 톤(비유·이모지) 유지하되 화면 흔들림·파티클 같은 과한 이펙트는 넣지 않는다.
9. 인라인 onclick은 이 프로젝트에서 **허용**(CSP 없음, 기존 관례). top-level 함수는 window에 자동 노출되므로 그대로 동작한다.

## 구조

```
src/index.html        마크업 전체 + css/js 매니페스트 (섹션 ①~⑭ + 배지·헤더)
src/css/style.css     전체 스타일 (팔레트 :root 변수 — hex 추가 전에 기존 변수 확인)
src/data/*.js         ★ 콘텐츠 (수정의 90%가 여기) — 파일별 스키마는 각 파일 첫 줄 주석
src/js/core.js        공용 유틸(shuffleArr)
src/js/sections/*.js  섹션 엔진 19개 (파일명 = 섹션)
tools/build.mjs       src → dist 인라인 빌더 (의존성 0)
tools/probe.template.html  검증 프로브 템플릿
dist/ml_dl_interactive.html  배포물 (빌드 산출 — 직접 수정 금지)
backup/               이식 전 원본
```

- 개발 중 미리보기: `src/index.html`을 그냥 브라우저로 열면 됨(classic script라 file://에서도 동작 — 빌드 없이 확인 가능).
- 대상 독자·문체·비유 사례: 교과서/모의고사/수능(검증 데이터), 양궁 코치(과적합), 오답 노트(부스팅), 담임 선생님(스태킹), 금붕어(RNN)/다이어리(LSTM) — 새 콘텐츠도 이 결로.

## 검증 방법 (이 PC 특이사항 포함)

- 이 PC: git 있음(원격 = github.com/jungbucks/ml-lecture, **main 푸시 = Pages 자동 배포** — 커밋·푸시는 사용자 몫), python은 스토어 스텁, 셸 PowerShell 5.1(`&&` 없음). Node v22 있음.
- 절차: `node tools/build.mjs` → ml-lecture 루트를 Node http 서버로 서빙 → `tools/probe.template.html`을 `dist/__probe.html`로 복사해 시나리오 작성 → 헤드리스 Edge `--dump-dom`으로 수확 → **__probe.html 삭제**.
- 회귀 기준: 2026-07-08 이식 검증에서 쓴 **24개 시나리오**(카드 그리드 수/플립/워크스루·stratify/버그찾기/퍼셉트론/스케일러/활성화/검증데이터/양궁 3상태/해석퀴즈/프로젝트/자가진단 15문항 채점·복사/베이즈/7단계/신경망 매칭 7/소프트맥스/kNN/트리/미니넷/LSTM·앙상블 표). 구조를 건드렸다면 전부, 콘텐츠만 바꿨다면 해당 섹션 + 자가진단만 돌려도 된다.
- 프로브 함정: top-level `const`는 `w.이름` 접근 불가(전역 어휘 스코프) — DOM에서 읽을 것. top-level `function`은 가능. 자가진단은 **문항 셔플+번호 재부여**(finalquiz.js가 `q`의 "N." 프리픽스를 떼고 재매김)라 문항 번호 문자열로 단언하지 말고 `.q-item` 개수+본문 키워드로 검사. 헤드리스 Edge는 `--user-data-dir`(임시 프로필) 없이는 기존 프로세스에 위임해 --dump-dom이 빈 출력이 됨.

## 이력

- 2026-07-15 (Fable, 5차): 리뷰 권고 8·9번 반영(⑤ 하이퍼파라미터 확장, 손실 용어 카드 뒤에 배치) — ⑧ **분류 평가지표 신설**(`#metrics-clf`): 혼동 행렬(암 진단 예, TP/FN/FP/TN 색 코딩) + 정확도/정밀도/재현율 정의·계산표 + 정확도의 함정(1,000명 중 10명 → 99%/재현율 0%, 심화 Q4와 상호 링크) + 선생님 요약(놓치면 큰일=재현율, 헛다리=정밀도). ⑨ **경사 하강법 체험존 신설**(`#gd`): **새 파일 2개** `data/gradient.js`(GD_LRS 학습률 프리셋 3단) + `js/sections/gradient.js`(u_new=u(1−2·lr) 공굴리기, 0.05 달팽이/0.3 수렴/1.05 발산, 궤적 점+발산 연출) — 매니페스트 등록 완료. **자가진단 19→20문항**(Q20 재현율 + FINAL_SEC 19→`#metrics-clf`, 제목·`/20` 동기화). 이로써 07-14 교사 리뷰 9개 항목 전부 소화(시험범위 무관 항목 기준).
- 2026-07-15 (Fable, 4차): 리뷰 권고 6·7번 반영 — ⑥ 스케일링 목적 문구 교정("계산을 쉽게"→"범위 큰 변수가 학습을 지배하는 것을 막고", 키/신발 사이즈 예시). ⑦ **강화학습 신설**(리뷰가 지목한 최대 커버리지 공백): ② 도감 끝에 `#rl` 서브섹션(강화학습 플립카드 `grid-rl`+AI↔환경 보상 루프 SVG, 지도/비지도/강화 3분류 비교표, 선생님 요약), 히어로 배지 5종째(rl), CSS에 `--rl` 팔레트(teal)+`.badge.rl`+`.c-rl` 신설, **자가진단 18→19문항**(Q19 강화학습 + FINAL_SEC 18→`#rl`, index 제목·`/19` 동기화 — finalquiz.js는 FINAL.length라 무변경). 프로브 17종 PASS(rl 카드·SVG·배지·3분류표·요약, 19문항 렌더·채점 생존·/19, 스케일링 문구, 4·5번 회귀, 기존 엔진 생존).
- 2026-07-15 (Fable, 3차): 리뷰 권고 4·5번 반영 — ④ **학습률 카드**: HYPER에 learning_rate 추가(optimizer 바로 뒤, 7장) — 검증 표·본문에 언급만 있고 정의가 없던 공백 해소. ⑤ **용어를 교과서 표기로 통일**(사용자 확인: 과대적합·과소적합·적합): '과적합'→'과대적합' 전면 교체(index 5곳·Ridge/Lasso 카드·ridge SVG·자가진단 Q16·주석 2곳), 양궁 상태 2 라벨 '최적적합'→'적합'. ⑤ 검증 카드에 "'과적합'이라고 부르는 책도 있어요" 병기 1회 유지. **이후 새 콘텐츠도 과대적합/과소적합/적합 표기 사용할 것.**
- 2026-07-15 (Fable, 2차): 리뷰 권고 3번 반영 — 활성화 함수 '은닉층용' 표의 계단 함수에 오개념 차단 각주 추가(index.html ④-3: 미분 불가→역전파 불가→현대 은닉층 미사용, ReLU가 계승). 퍼셉트론 시뮬(④-1)의 계단함수는 초기 퍼셉트론 재현이므로 무변경.
- 2026-07-15 (Fable): 교사 관점 전수 리뷰(데이터 9종+index 전체) 후 오류 2건 수정 — ① walkthroughs.js scaler 해설의 죽은 참조 "버그 찾기 ④-2번"→"섹션(⑦)의 버그 2번"(재매김 잔존물). ② 규칙 6 동기화 쌍의 스테일 섹션 번호(⑦→④-4, ⑧→①) 교정 + 현행 섹션 번호표·재배치 시 갱신 의무 명시. 리뷰의 나머지 발견(계단함수 각주·학습률 카드·과적합 용어 통일·스케일링 목적 문구·강화학습 부재·정밀도/재현율 승격)은 시험범위(ROADMAP U3) 확정 후 우선순위 결정 대기.
- 2026-07-14 (Fable): 공개 전 점검 정리 — ① 관문 비밀번호 평문을 CLAUDE.md에서 마스킹(공개 저장소 노출 방지, 코드에는 원래 해시만 있어 무변경). ② 배포 경로 현행화(Pages+Actions가 주 배포, dist는 보조, 홈 사본은 스테일 처리). ③ `.gitattributes`(`dist/** -text`) 추가 — 빌드 때마다 EOL 정규화로 dist가 가짜 변경 표시되던 노이즈 제거. 콘텐츠·엔진 무변경.
- 2026-07-09 (Fable, 7차): **교육자 관점 점검 반영(정확성 5건 + 커버리지)**. ① project.js 5단계 예시 데이터 누수 수정("2010년부터"→"2011년부터 테스트 데이터로", 3단계 분할과 정합). ② 퍼셉트론 편향 표기 `w₀=θ`(부호 오개념) 제거 → 편향 `b` + 계단함수 임계값을 0으로 통일(SVG 노드·구조문·활성화표·perceptron.js P_FWD_MSG ③⑤). ③ 베이즈에서 '우도(Likelihood)' 용어 전면 제거(고교 수준 단순화, 사용자 지시) → P(A|B)는 '조건부 확률'로만. index 3곳(SVG 라벨·기호표·슬라이더) + quizzes Q13 옵션·해설 동기화. ④ MinMaxScaler "[0,1] 또는 [-1,1]" → "기본값 [0,1](옵션 지정 시 [-1,1])"(index ③표·bugs.js why). ⑤ 스케일링 '압축' 표현 제거(PCA 차원축소와 혼동 방지) → steps7[3]·index ① 목록(동기화 쌍) 및 scaling 섹션 intro 모두 '범위 조정'으로. **자가진단 15→18문항**: 신규개념 3문항(Ridge/Lasso 규제·DBSCAN·PCA) 추가 + FINAL_SEC 15~17(algos) + index 제목/`/18` 동기화(finalquiz.js는 FINAL.length라 무변경). **심화(⑪) 개편**: 정리노트 .txt 다운로드 전면 삭제(vault.js `vaultDownload` 함수 + 버튼 + 헤더 주석), 통찰형 심화문제 Q4(정확도의 함정·불균형 데이터·정밀도/재현율)·Q5(데이터 편향·분포 이동) 추가. 프로브 22/22 PASS(누수수정·편향b·우도전멸·MinMax·압축제거·18문항 렌더·채점동작·오답노트·다운로드제거·Q4Q5·gateTry 생존). ⚠️ 관문(gateTry)·F12 억제는 무손상 확인.

- 2026-07-09 (Fable, 6차):

- 2026-07-09 (Fable, 6차): **딥러닝 4섹션 통합 + 메뉴 축소(14→11)**. ④퍼셉트론·⑤낱말사전·⑥활성화·⑦신경망종류 → 단일 `<section id="dlintro">` "④ 딥러닝 입문"의 하위 주제 1~4(`.dlsub`+`h3.subsec` 신설, **기존 id 4개는 dlsub div로 보존** — 오답노트·앵커 호환). h2 ①~⑪ 재매김, nav 4링크→"딥러닝 입문" 1링크, "성능 손잡이"→"하이퍼파라미터"(h2는 "⑤ 하이퍼파라미터 — 모델의 성능 조절 손잡이"로 순서 교체). 상호참조 동기화: FINAL_SEC(5~9·11 라벨), walkthroughs 섹션(⑩→⑦)×2, archery 주석(⑧→⑤). **플립카드 겹침 수정**: `.face.back .tap`을 absolute→static(흐름 배치) — 뒷면은 overflow:auto 스크롤 컨테이너라 absolute면 긴 설명(Lasso·DBSCAN)과 겹쳤음. **⑦→④-4 신경망 표 정제**: 3열 설명을 1문장 핵심형으로 축약(★NEW 배지 제거, ANN_MATCH 게임 문구와 결 일치 — 게임 데이터는 무변경). 프로브 20종 PASS(nav 구성·h2 연속성·앵커 보존·넘버링·엔진 생존·tap static·표 문구·FINAL_SEC). nav의 12번째 `<a>`는 인스타 브랜드 링크(정상).
- 2026-07-09 (Fable, 5차): **섹션 전면 재배치(교육 흐름) + 학생 친화 이름**. 새 순서: ①문제해결7단계(steps7) ②알고리즘 도감(algos) ③데이터 다듬기·스케일링 ④퍼셉트론 ⑤신경망 낱말사전 ⑥활성화 함수 ⑦신경망 종류 ⑧성능 조절 손잡이·하이퍼파라미터 ⑨코드 한 줄씩 읽기 ⑩버그 찾기 ⑪코드 읽기 퀴즈 ⑫AI 프로젝트 ⑬실력 점검·자가진단 ⑭보너스·심화. "큰 그림(7단계)→ML→딥러닝→코드실습→정리" 흐름. h2·nav 전부 친화적 이름으로. 상호참조 재동기화: walkthroughs 섹션(③→⑩), steps7 주석(⑦→①), archery 주석(⑪→⑧), FINAL_SEC 전면. 프로브 15/15 PASS(순서·번호·nav앵커·카드·베이즈체험존·스팸필터·양궁·7단계게임·워크스루참조·자가진단). ⚠️ **재배치 함정(반드시 지킬 것)**: 섹션 이동 시 ①건드리면 안 되는 하위번호(perceptron.js 순전파 ①~⑥, walkthroughs 코드단계 ①~④, index 체험존 ①②③)와 ②재매김 대상(h2 섹션번호)을 구분. 상호참조 4곳(FINAL_SEC·walkthroughs 섹션()·steps7주석·archery주석) 동기화 필수. 스크립트로 섹션 추출→h2교체→재조립→nav재구성 권장.
- 2026-07-09 (Fable, 4차): 나이브 베이즈 재구성 — 독립 ② 베이즈 공식 섹션을 없애고 **알고리즘 섹션의 체험존 ③(스팸 필터 계산기+공식표)**으로 이동(#bayes id는 wrapper로 유지해 앵커·오답노트 링크 보존). 나이브 베이즈는 분류 카드로 그대로. **h2 번호 전체 재매김**(기존 ③~⑮ → ②~⑭), nav "베이즈 공식" 링크 제거. 상호참조 동기화: walkthroughs 섹션(④)→(③), steps7 주석 ⑧→⑦, quizzes FINAL_SEC 번호·베이즈 라벨. 프로브 14/14 PASS. ⚠️ **번호 재매김 함정**: h2의 ①②③은 순서 마커라 섹션 이동 시 반드시 재매김 + FINAL_SEC(quizzes.js)·walkthroughs 섹션참조·steps7 주석까지 동기화. 미결(사용자 확인 대기): 원 요청의 "7단계를 맨 위로" 재배치 + 메뉴 이름 교육적 개선은 범위 미확정으로 보류.
- 2026-07-09 (Fable, 3차): 콘텐츠 대확장 + 지도학습 카드 SVG 리뉴얼(전부 `data/algo-cards.js` + index.html algos 섹션). **비지도 3종**: K-Means에 DBSCAN(밀도·노이즈)·PCA(차원축소) 카드 추가(grid-unsup 1→3) + 도넛 K-Means vs DBSCAN 비교 SVG 도해 + 선생님 요약 팁. **회귀 3종**: grid-pred(1→3)에 다항 회귀·Ridge/Lasso(규제) 추가, "예측(회귀)" 제목·안내문. **SVG 리뉴얼**: 지도학습 6종(linreg·knn·tree·logi·svm·bayes) 축/그라데이션/그림자/음영 추가로 세련화 + 신규 4종(polyreg·ridge·dbscan·pca). 프로브 14/14 PASS(카드 수 3/5/4/3·새 카드 문구·도해·팁·회귀 안내·회귀 무손상). 함정 메모: `.demo-title` 클래스는 이 프로젝트에 없음(다른 페이지 것) — 인라인 스타일로 대체함. 카드 추가는 SVGS·ALGO_CARDS 두 곳 동시 수정.
- 2026-07-09 (Fable, 2차): 섹션 잠금 → **페이지 전체 접근 관문**으로 전환(교사 공유용). `<body class="gated">`+`#pageGate` 오버레이가 첫 페인트부터 본문을 가리고(FOUC 방지: `body.gated > *:not(#pageGate){visibility:hidden}`), 정답 시 해제. **sessionStorage(`mll_gate_ok`)로 세션 내 재입력 방지**(새로고침해도 그 탭은 통과). `gateTry()`가 관문 로직, 심화 섹션 ⑮는 내부 잠금 제거하고 관문 통과 후 보이는 일반 콘텐츠(심화 3문제+정리노트 .txt 다운로드)로 남김. F12 억제·정직 안내·올바른 해시(881bcf26…)는 유지. 로직은 `js/sections/vault.js`. 프로브 14/14 PASS(초기 잠금·본문 숨김·오답·정답·세션 유지·회귀·원문 비노출).
- 2026-07-09 (Fable, 1차): ⑮ 심화 자료 게이트형(섹션) 잠금 추가 — 위 2차에서 페이지 관문으로 대체됨. ⚠️ 교훈: 요청서 해시(5a8053a4…)가 관문 원문의 실제 SHA-256과 불일치 → 원문 기준 올바른 해시(881bcf26…)로 교체함. (원문 평문은 공개 저장소라 이 문서에 적지 않는다 — 사용자가 알고 있음) **관문 뒤 콘텐츠에 진짜 비밀 넣지 말 것**(F12로 우회됨 — 공유용 문지기일 뿐).
- 2026-07-08 (Fable): 단일 HTML(1,900줄)을 본 구조로 이식. JS를 블록 단위 정확 분할(조각 재결합==원본 바이트 일치 검증) 후 데이터 9파일 + 엔진 21파일로 분리. build.mjs(인라인+3중 검문) 신설. dist 회귀 프로브 24/24 PASS, src도 http/file:// 양쪽 24/24·렌더 확인. 홈의 기존 파일은 dist 사본으로 교체, 원본은 backup/에 보존.
- 이식 전 이력(같은 날): stratify 해설 추가, 검증 데이터(모의고사) 카드, 과적합 양궁 시뮬, 자가진단 10→15문항+결과 요약 복사(활동지), 앙상블 XGBoost·Stacking+구분표, 신경망 표 위트 개편+LSTM. "부교재 원본 오타" 문구 전량 제거(부교재 교체됨).
